This script is just execute specific script according to config file
#If we want to compile python file to executable binary file, using below command
pyinstaller -c -F run.py
#The method to install pyinstaller
pip install pyinstaller
